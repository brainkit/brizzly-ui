import ElCheckboxTag from '../checkbox/src/checkbox-tag.vue';

/* istanbul ignore next */
ElCheckboxTag.install = function(Vue) {
  Vue.component(ElCheckboxTag.name, ElCheckboxTag);
};

export default ElCheckboxTag;
